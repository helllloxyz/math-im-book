User:
目前的仓库生成书籍的逻辑，是自上而下的，
有一点原则我想改下， 大的思路还是自上而下，但是我希望不要一次生成，更多的是跟用户交互过程中逐渐生成
先列主要的思想，后续根据用户的问题去选择生成方向，就像一颗树一样，不断的分叉
鉴于知识之间广泛的联系性，在生成过程中得考虑context的维护，一个好的思路就是学习skill的思路，markdown的文件，head是简略描述，不断的卸载不需要的部分，load需要的部分

所以有个知识性markdown的管理， skill markdown管理有什么好的方案吗


User:
数学类需要考虑定义符号和标记的一致性问题，虽然同一个模型可能比较一致
不要过于复杂，不够灵活智能，也许可以考虑加个  /compact指令，自动整理整个内容


User:
有什么好的前端交互设计参考呢，一方面是网络状的知识文本，一方面是命令行式的交互逻辑


User:
有个新的想法: """## 基本原则
- 以对话为基础
- agent思路，需要维护一个context(state + memory）

context中包含
1. 符号表
2. markdown表示的知识定义list， 树状层次结构
3. 当前激活的chat的节点关系 (因为有fork)

## 使用流程
使用方法先是用户提问(任何需求)，检查已经有的知识树，触发下面 
1. 拉取相关知识， 直接回答问题，
2. 需要扩展知识树， 生成一份清单 先生成相关内容(markdown落盘)，再回答问题

历史各种对话保留， 用于分析用户的偏好， 可以开新的对话

核心是维护知识库，知识库变成了一份类似书籍的文本

chat list
markdown list
回答问题时可以引用markdown中的片段，避免文档的重复

可以选中内容继续问，选中内容问会自动fork，避免上下文过大"""